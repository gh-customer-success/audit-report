name: User Audit action
on:
  workflow_dispatch:
jobs:
   Repo-Permissions:
    runs-on: ubuntu-latest
    steps:
      -
        name: run audit
        uses: gh-customer-success/user-audit@193885db78b47c2fd98c96e62973f8efabc59f4b
        with:
          api_token: ${{ secrets.OCTODEMO_REPO_AUDIT }}
          owner: ${{ vars.OWNER }}
          repo: ${{vars.repo}}
        id: audit
      -
        name: print output
        run: |
          echo "TEAMS ${{steps.audit.outputs.teams}}"
          echo "REPOS ${{steps.audit.outputs.repos}}"
