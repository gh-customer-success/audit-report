name: User Audit action
on:
  workflow_dispatch:
jobs:
   Repo-Permissions:
    runs-on: ubuntu-latest
    steps:
      -
        name: run audit
        uses: gh-customer-success/user-audit@f2d3a92f30123085093c94536f27aaf7b001e8aa
        with:
          api_token: ${{ secrets.OCTODEMO_REPO_AUDIT }}
          owner: ${{ vars.OWNER }}
          repo: ${{vars.repo}}
        id: audit
      -
        name: print output
        run: |
          echo "TEAMS ${{steps.audit.outputs.teams}}"
          echo "REPOS ${{steps.audit.outputs.repos}}"
