name: User Audit action
on:
  workflow_dispatch:
jobs:
   Repo-Permissions:
    runs-on: ubuntu-latest
    steps:
      -
        name: run audit
        uses: gh-customer-success/user-audit@6fdfbcf650671b58a33d8a0706c3a412307bb88c
        with:
          api_token: ${{ secrets.OCTODEMO_REPO_AUDIT }}
          owner: ${{ vars.OWNER }}
          repo: ${{vars.repo}}
        id: audit
      -
        name: print output
        run: |
          echo "TEAMS ${{steps.audit.outputs.teams}}"
          echo "REPOS ${{steps.audit.outputs.repos}}"
